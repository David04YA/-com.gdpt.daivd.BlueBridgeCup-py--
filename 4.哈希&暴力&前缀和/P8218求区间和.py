import sys 
input = lambda :sys.stdin.readline().strip()

n = int(input())
a = list(map(int,input().split()))
q = int(input())

#a[L]+..+a[R]
#p[R+1]-P[l]
#P[r]-P[l-1]
#a[l-1]+..+a[R-1]
for i in range(q):
    l,r = map(int,input().split())
    #l,r代表传入的区间端点,如输出样例中
#     4
# 4 3 2 1
# 2
# 1 4 l r
# 2 3 l r
    print(p[r]-p[l-1])

# n:

# 代码: n = int(input())
# 含义: 它代表你要处理的序列的长度，也就是序列中一共有多少个数字。
# 对应输入: 在样例中，第一行是 4，所以 n 的值就是 4。
# a:

# 代码: a = list(map(int,input().split()))
# 含义: 它是一个列表（list），存储了题目给定的原始数字序列。
# 对应输入: 在样例中，第二行是 4 3 2 1，所以 a 列表就是 [4, 3, 2, 1]。
# q:

# 代码: q = int(input())
# 含义: 它代表接下来会有多少个问题（查询）。
# 对应输入: 在样例中，第三行是 2，所以 q 的值是 2，意味着程序需要回答两个关于区间和的问题

# 题目的含义是：

# 给你一串数字：比如，一个星期的每日步数 [8000, 9000, 7500, 12000, 8500, 11000, 13000]。
# 给你几个问题：每个问题都是一个时间段，比如：
# 问题1：“从第2天到第4天，总共走了多少步？”
# 问题2：“从第5天到第7天，总共走了多少步？”
# 你的任务：对每一个问题，都计算出那个时间段内所有数字的总和，并把答案告诉提问者。
# 对应到题目中的术语：

# 序列 a：就是那一串给定的数字。
# n：是这串数字的总个数。
# 区间 [l, r]：就是一个问题，问你从第 l 个数加到第 r 个数的和是多少。
# m：是问题的总数量。 q